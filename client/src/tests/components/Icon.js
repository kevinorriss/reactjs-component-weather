/*
import React from 'react'
import { mount } from 'enzyme'
import renderer from 'react-test-renderer'
import { Icon } from '@kevinorriss/weather'

it('should render clear-day Icon correctly', () => {
    const component = renderer.create(<Icon name="clear-day" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render clear-night Icon correctly', () => {
    const component = renderer.create(<Icon name="clear-night" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render rain Icon correctly', () => {
    const component = renderer.create(<Icon name="rain" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render snow Icon correctly', () => {
    const component = renderer.create(<Icon name="snow" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render sleet Icon correctly', () => {
    const component = renderer.create(<Icon name="sleet" />).toJSON()
    expect(component).toMatchSnapshot()
})


it('should render wind Icon correctly', () => {
    const component = renderer.create(<Icon name="wind" />).toJSON()
    expect(component).toMatchSnapshot()
})


it('should render fog Icon correctly', () => {
    const component = renderer.create(<Icon name="fog" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render cloudy Icon correctly', () => {
    const component = renderer.create(<Icon name="cloudy" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render partly-cloudy-day Icon correctly', () => {
    const component = renderer.create(<Icon name="partly-cloudy-day" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render partly-cloudy-night Icon correctly', () => {
    const component = renderer.create(<Icon name="partly-cloudy-night" />).toJSON()
    expect(component).toMatchSnapshot()
})

it('should render default Icon correctly', () => {
    const component = renderer.create(<Icon name="anyvaluethatdoesntexist" />).toJSON()
    expect(component).toMatchSnapshot()
})


clear-day
clear-night
rain
snow
sleet
wind
fog
cloudy
partly-cloudy-day
partly-cloudy-night
default
*/